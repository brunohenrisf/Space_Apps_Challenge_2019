# Credenciais da casa do cliente — nunca sobem
firmware/*/config.h

# Saída de build: gerada por `node tools/build.mjs`
dist/

node_modules/
.DS_Store

# Servidor do Raspberry Pi
server/node_modules/
server/config.json
server/dados/

# O .env é da casa, não do repositório
.env

# Capturas que os bancos de ensaio (Playwright) deixam cair na raiz
/d[0-9]-*.png
/v1-*.png
/r-[abc].png
